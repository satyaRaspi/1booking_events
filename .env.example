# Optional. Leave blank for single Railway same-origin deployment.
ONEBOOKING_ALLOWED_ORIGINS=

# Optional custom log directory. Railway filesystem is ephemeral unless you add a volume.
ONEBOOKING_LOG_DIR=/app/1booking_data

# Optional QR secret for demo. Set a strong value before any public demo.
ONEFACEID_QR_SECRET=change-this-secret
