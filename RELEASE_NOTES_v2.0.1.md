# 1Booking v2.0.1 — Railway Frontend Root Fix

## Fix

- `/` now serves the React frontend on Railway.
- API health endpoint moved to `/api/health`.
- `railway.json` healthcheck path updated to `/api/health`.

## Default login

- Username: `admin`
- Password: `admin123`

